What are the types of functions in python?
    1.Built-in - range(), Print(), input(), len(), ect
    2.User defined - using def keyword
    3.Lamda - using lamda keyword
    4.Recursive - Functions calling itself

What are user defined functions?
    A function is a block of code that runs when called or involed

What is the syntax to define a function?
    def function_name(parameter):
        block of code that runs

What is the syntax to call a function?
    function_name(argument)

What is pass keyword
    It is a pre defined keywordused to create empty functions
    Example:
        def function_name(argument):
            pass
        function_name()    

What are the types of buid in functions in python?
    Type 1: Functions with no arguments
    Type 2: Functions with arguments and parameters
    Type 3: Keyword arguments
    Type 4: Default arguments 